# Firecrawl - points at the locally-running firecrawl-local Docker Compose stack.
# USE_DB_AUTHENTICATION=false so any non-empty string works as the key.
FIRECRAWL_API_KEY=local
FIRECRAWL_API_URL=http://localhost:3002
